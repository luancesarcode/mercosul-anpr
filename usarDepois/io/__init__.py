"""I/O package files kept for architecture parity."""

# NOTE:
# Python's standard library already provides a top-level module named `io`.
# To avoid import conflicts in runtime code, this project imports from `io_layer`.
