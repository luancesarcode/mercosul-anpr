# NVIDIA profile (CUDA 12.4) for Linux and Windows.
# A recent compatible NVIDIA driver is required. Use requirements-cpu.txt
# when installing on a machine without an NVIDIA GPU.
--extra-index-url https://download.pytorch.org/whl/cu124

-r requirements-common.txt
torch==2.5.1+cu124
torchvision==0.20.1+cu124
